# 🏥 보험 Q&A 챗봇

Streamlit 기반의 보험 약관 Q&A 챗봇입니다. Solar LLM 모델과 Qdrant 벡터 데이터베이스를 사용합니다.

## 🚀 빠른 시작

### 1. 필수 환경 설정

```bash
# 프로젝트 디렉토리로 이동
cd c:\Users\MiniH\Desktop\Langchain-Project\git\langchain-llm-langchain-1-7-students\langchain_project

# Poetry 의존성 설치
poetry install
```

### 2. Qdrant 서버 시작

```bash
# Docker에서 Qdrant 실행
docker run -d --name qdrant -p 6333:6333 qdrant/qdrant
```

### 3. 애플리케이션 실행

```bash
poetry run streamlit run source/app/app_streamlit.py
```

브라우저에서 `http://localhost:8501` 접속

## 📁 프로젝트 구조

```
langchain_project/
├── source/
│   ├── app/
│   │   └── app_streamlit.py       # 메인 애플리케이션
│   ├── ingest/
│   │   └── ingest_simple.py       # 데이터 수집 스크립트
│   ├── llm/
│   │   ├── llm.py                 # LLM 설정
│   │   └── prompt.py              # 프롬프트 템플릿
│   ├── vectorstore/
│   │   ├── qdrant_client.py       # Qdrant 클라이언트
│   │   └── retriever.py           # 검색 엔진
│   ├── chains/
│   │   └── qa_chain.py            # Q&A 체인
│   └── config/
│       └── settings.py            # 설정
├── .files/                        # 보험 약관 XML 파일
├── .env                           # 환경 변수 (API 키)
└── pyproject.toml                 # 의존성 정의
```

## 🔧 주요 기능

- **보험 약관 검색**: 5개 보험 상품의 672개 문서 검색
- **자동 응답**: Solar LLM을 사용한 자연스러운 답변
- **출처 표시**: 답변의 출처 문서 확인 가능
- **대화 히스토리**: 세션 중 이전 대화 조회

## 📝 사용 방법

1. Streamlit 앱에서 질문 입력
2. 엔터 또는 "질문하기" 버튼 클릭
3. 자동으로 관련 보험 약관에서 검색
4. AI가 생성한 답변 확인

## ⚙️ 설정 변경

### API 키 설정
`.env` 파일에서:
```
UPSTAGE_API_KEY=your_api_key_here
```

### LLM 모델 변경
`source/llm/llm.py`에서 모델명 수정

## 🔍 데이터 재수집

보험 약관 데이터를 다시 수집하려면:

```bash
poetry run python source/ingest/ingest_simple.py
```

## 📊 지원하는 보험

- 상해보험 (79개 문서)
- 손해보험 (189개 문서)
- 질병보험 (105개 문서)
- 책임보험 (188개 문서)
- 화재보험 (111개 문서)

**총 672개 문서**

## 🛠️ 문제 해결

### Qdrant 연결 안 됨
```bash
# Docker가 실행 중인지 확인
docker ps

# 없다면 다시 시작
docker run -d --name qdrant -p 6333:6333 qdrant/qdrant
```

### API 키 오류
- Upstage 콘솔에서 크레딧 확인: https://console.upstage.ai/billing
- `.env` 파일의 API 키 확인

## 📚 관련 파일

- `pyproject.toml` - 패키지 의존성
- `poetry.lock` - 정확한 버전 정보
- `.gitignore` - Git 제외 파일

---

**마지막 업데이트**: 2024년
